# 1.10 - Providers

providers/ — the QwenCloud provider module. DISABLED; kept on disk in case pi's native provider support proves weaker.

<!-- last-reviewed: 2026-08-04 20:37 -->

## References

- Master: [project_documentation.md](../project_documentation.md)
- Full project map: [project_map.md](../project_map.md)
- Map: [project_map.md](../project_map.md)

## Purpose

**Owns:** `qwencloud.ts` — Alibaba Qwen Cloud (DashScope) + Qwen Coding Plan registration via pi's `/login`.
**Does not own:** anything active — the module is not wired in `index.ts`.
**Depends on:** 1.1 (`pi.registerProvider`). **Dependents:** none.

## Public API & contracts

`createProviders(pi): void` — commented out in the orchestrator (both the import and the call; re-enable both lines to restore). Registers providers with live catalogs, cache/seed fallbacks, OAuth login flow, and a `/qwencloud` command. Its help-registry entries stay wired (they register only if the module loads; an empty category is skipped on the help screen).

## Internal architecture & data flow

Disabled 2026-07 when pi 0.81 added native provider support (native `/login`, model picker). Domain/base-URL preferences remain in `config.json` (`qwencloudCloudDomain`, `qwencloudCloudApiFormat`, `qwencloudPlanApiFormat`, `qwencloudPlanOpenAI`, `qwencloudPlanAnthropic`).

## Configuration

The `qwencloud*` preferences above (dormant while disabled).

## Setup, seeding & first run

Re-enable in `index.ts`, `/reload`.

## Testing

`tests/qwencloud-check` (heuristics, catalog parsing, registration shape, live refresh, fallbacks, oauth flow).

## Operational notes & known limitations

- Before building/re-enabling a custom provider, check whether pi already ships it natively (that is why this module is off).

## Related

- 1.1 pi Extension Runtime
