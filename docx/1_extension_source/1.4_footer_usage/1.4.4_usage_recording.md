# 1.4.4 - Usage recording (usage-recording.ts)

<!-- last-reviewed: 2026-08-09 22:30 22:05 -->

## References

- Master: [project_documentation.md](../../project_documentation.md)
- Full project map: [project_map.md](../../project_map.md)
- Map: [1.4_footer_usage_map.md](./1.4_footer_usage_map.md)

## Function

Implements the `TurnRecorder` interface (types.ts) that core.ts (1.4.1)
calls on every assistant `message_end` and every settled task: writes one
metrics row per turn into the shared SQLite DB (1.2 `db.ts`). No commands,
no UI. Source: `usage-recording.ts` (185 lines). Does not import core.ts or
usage-report.ts.

## What is recorded

Per-turn METRICS and prompt-type CLASSIFICATION FLAGS only — never prompt
or response text, file paths, tool names/arguments or thinking content
(those live in pi's session JSONL). ~100 bytes per row.

Columns written per turn row (20 total): `turn, timestamp, session_id,
prompt_index, model_name, thinking_level, thinking_ms, response_ms,
cost_usd, input_tokens, output_tokens, cache_read, cache_write` + flags
`user_prompt, base_prompt, sub_prompt, steering_prompt, followup_prompt,
continuation_prompt` + `prompt_kind` ("base" | "continuation" | "steer" |
"followup" | "auto").

Task rows (via `recordTask` from core's `agent_settled`): `session_id,
prompt_index, timestamp, task_ms, stop_reason ('complete'|'error'|
'aborted'|'toolUse'), model_name, thinking_level, turn_count`.

## Recording policy

- Zero-cost turns (free models / $0 subscription turns) ARE recorded —
  their counts/timings are useful; the report keeps them out of COST
  averages with paid-only denominators. `RECORD_ZERO_COST_TURNS` (module
  constant) can skip them entirely.
- Negative-cost turns are never recorded (defensive).
- The in-memory footer accumulator always updates regardless, so the live
  footer shows the real last-turn cost.

## Runtime states

- DB unavailable (better-sqlite3 missing): `recordTurn`/`recordTask` are
  no-ops; footer still works; `/aftc-install` fixes (1.5.3).

## History note

Previously owned `/show-thinking` + `/hide-thinking` (footer line-3 timing
visibility) — removed; pi's native Ctrl+T covers thinking visibility.

## Related

- Schema owner: [1.2_core_infrastructure_documentation.md](../1.2_core_infrastructure_documentation.md) · Producer: [1.4.1_cache_core.md](./1.4.1_cache_core.md) · Consumer: [1.4.5_usage_report.md](./1.4.5_usage_report.md)
